{
    "name": "@trustcart/shared",
    "version": "0.0.1",
    "private": true,
    "description": "Shared types and utilities for TrustCart Kenya",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "type-check": "tsc --noEmit",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "rimraf": "^5.0.5",
        "typescript": "^5.3.3"
    }
}